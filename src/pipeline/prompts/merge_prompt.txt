You are an expert in data extraction and text structuring for historical German documents (1920-1945).
You will receive a JSON array containing line-by-line OCR transcriptions of consecutive pages from ONE single German film approval card ("Filmprüfstelle Berlin").

Task:
Extract all film metadata, cast, examination board details, and technical properties from the provided page transcriptions and populate the required output schema.

MANDATORY RULES:

1. Language Integrity (CRITICAL):
   - Schema keys are defined in English, but ALL extracted values MUST remain strictly in their original German phrasing as transcribed. Do not translate any text.

2. Strict Fidelity & Zero Hallucination:
   - Extract ONLY information that is explicitly stated in the source text.
   - Do NOT invent, assume, or complete missing names, dates, or numbers.
   - If a specific field is not mentioned anywhere in the transcriptions, set its value to null.

3. Cross-Page Merging:
   - Merge text spans that are split across page breaks (e.g., actor names, decision texts, or notes) into single continuous strings without artificial line breaks.

SOURCE DATA:
{PAGES_JSON}